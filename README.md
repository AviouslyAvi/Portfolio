# Avi — The Alchemist (Portfolio)

Preview locally:

1. Open `c:\Users\Avious\VSCODE\portfolio\index.html` in your browser.
2. Or serve the folder using a static server (recommended):

```bash
# from c:\Users\Avious\VSCODE\portfolio
python -m http.server 8000
# then open http://localhost:8000
```

Files:
- `index.html` — main page
- `styles.css` — styles
- `script.js` — small behavior

Next steps I can do for you:
- Add audio examples and embedded players
- Replace the placeholder email with your real contact
- Create downloadable EPK and social links
